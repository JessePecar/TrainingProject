<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TFTHelper2.Mobile.UI.Views.MasterDetails.MasterDetailPageDetail"
             Title="Home">
    <Page.Resources>

    </Page.Resources>
    <CarouselView ItemsSource="{Binding ContentHolders}" CurrentItem="{Binding SelectedView}">
        <CarouselView.ItemTemplate>
            <DataTemplate>
                <ContentView Content="{Binding .}"></ContentView>
            </DataTemplate>
        </CarouselView.ItemTemplate>

    </CarouselView>
</ContentPage>
